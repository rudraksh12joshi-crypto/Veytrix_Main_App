import React from "react";
import { View } from "react-native";

// Renders a selection outline for currently selected clips/tracks.
export function TimelineSelection() {
  return <View testID="timeline-selection" />;
}
