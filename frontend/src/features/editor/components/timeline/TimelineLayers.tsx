import React from "react";
import { View } from "react-native";

// Visual grouping of layered clips (picture-in-picture, overlays).
export function TimelineLayers() {
  return <View testID="timeline-layers" />;
}
