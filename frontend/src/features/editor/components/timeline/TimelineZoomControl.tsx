import React from "react";
import { View } from "react-native";

// Zoom control affordance (slider + fit-to-window button).
export function TimelineZoomControl() {
  return <View testID="timeline-zoom-control" />;
}
